APP_ENV=testing
# Clave de pruebas. Genera la tuya con: php artisan key:generate --show
APP_KEY=
DB_CONNECTION=sqlite
DB_DATABASE=:memory:
# Explicitly cleared: config/database.php's connection blocks read DB_URL and
# (before this file existed with this line) would silently override the
# chosen driver/host with whatever DB_URL points to. Kept empty here as a
# second line of defense even though config/database.php's sqlite block no
# longer declares a 'url' key at all.
DB_URL=
BCRYPT_ROUNDS=4
CACHE_DRIVER=array
SESSION_DRIVER=array
QUEUE_CONNECTION=sync
MAIL_MAILER=array
